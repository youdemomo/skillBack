server:
  port: 28080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ssm_demo?serverTimezone=UTC&characterEncoding=utf8
    username: root           # 根据你的数据库用户名修改，默认root
    password: 123123123   # 数据库密码，替换为实际密码
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.seckill_backend.model
